version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations" # goose .sql files double as schema
    queries: "internal/repo/query.sql"
    gen:
      go:
        package: "repo"
        out: "internal/repo"
